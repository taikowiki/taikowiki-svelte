import type { LangFile } from "../types";

const en:LangFile = {
    layout: {
        main: {
            song: 'Songs',
            doc: 'Docs'
        }
    },
    '/song':{
        placeholder: 'Search'
    }
}

export default en;