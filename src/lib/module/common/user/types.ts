export interface UserBasicData {
    order: number;
    provider: string;
    providerId: string;
    registerTime: number;
    grade: number;
}

export interface UserData{
    order: number;
    provider: string;
    providerId:string;
    UUID: string;
    nickname:string;
}